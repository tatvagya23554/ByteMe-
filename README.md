# ByteMe
Valid customer user name for simulation: customer@IIITD
Valid customer password for simulation: customer_2024iiitd

I wrote the code on IntelliJ(Project name: a4 & Package name: assignment_4) and for checking out the aptness of my test cases you need to include the corresponding necessary .jar files in your project. I am attaching these .jar files in a separate folder inside this .zip file. Proceed as follows:
File -> Project Structure -> Modules -> Dependencies -> "+" icon -> add all the .jar files -> Apply -> OK

Note: Simulation for each functionality has been duly made. Any assumptions, if made, are explicitly and duly commented. Also, a sample run of the program has been provided within the main function. I am attaching empty files named Customers.txt and Orders History.txt for your convenience.
